# Type Aliases

- Similar to how `typedef` works in **C** , the `type` keyword allows you to delineate a user defined type 


```typescript
type Age : number ; 
type Person = {
    age : number ;
    name : string ;
}; 

```
